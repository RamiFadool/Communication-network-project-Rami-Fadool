DNS_SERVER_HOST = "127.0.0.1"
DNS_SERVER_PORT = 5556

# max number of bytes to recieve in one packet
BUFFER_SIZE = 1024

# the domain name that the client asks for
DOMAIN_NAME = "www.ariel.ac.il"

# the IP address returned for the domain
DOMAIN_IP = "127.0.0.1"